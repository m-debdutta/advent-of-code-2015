const { describe, it } = require('node:test');
const assert = require('assert');

describe('LightController', () => {
  describe('instruct', () => {
    it('should instruct the lights to change their stats', () => {
      
    });
  });
});